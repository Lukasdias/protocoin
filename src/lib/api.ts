import { UserProps } from '../utils/user.props'

export const post = (key: string, element: UserProps) => {
  return
}

export const get = (key: string, element: UserProps) => {
  return
}

export const remove = (key: string, element: UserProps) => {
  return
}
